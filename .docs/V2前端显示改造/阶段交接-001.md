# NyaaLibrary-MCP V2 前端显示改造 阶段交接 001

## 交接目的

记录 P1 阶段的完成状态，为下一阶段 P2（后端路由）提供续接上下文。

**续接前必读文档**：
- `NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md` — 全量 SSOT 计划
- `NyaaLibrary-MCP\.docs\V2前端显示改造\设计审核报告.md` — 设计审核与关键决策
- `NyaaLibrary-MCP\CLAUDE.md` — 项目规则

## 当前进度

P1 ✅ — 后端 adminMemory service 层完成。

### 已实现（P1）

| 交付物 | 文件 | 行数 |
|--------|------|------|
| 类型 `OwnerSummary` `MemoryEntryRow` | `server/src/services/adminMemory.ts` | L15-29 |
| `listOwners()` — 聚合 owners（FULL OUTER JOIN） | 同上 | L33-47 |
| `listMemoryByOwner(ownerKey)` — 列 owner 记忆 | 同上 | L51-61 |
| `deleteMemoryById(ownerKey, id)` — 精确删除 + 越权防护 | 同上 | L67-97 |
| `deleteProfile(ownerKey)` — 删除画像 | 同上 | L101-107 |
| `dimensionInSync()` — 维度一致前置检查 | 同上 | L111-119 |

### 验证结果

18/18 项全部通过：
- TypeScript 编译零错误
- `listOwners` FULL OUTER JOIN 正确聚合（有记忆无画像的 owner 也出现在列表中）
- `listMemoryByOwner` 按 `created_at DESC` 正确排序
- `deleteMemoryById` 越权防护：错误 owner → `false`，不碰 vec_mem/mem_fts
- `deleteMemoryById` 正确 owner：三表（memory_entries / vec_mem / mem_fts）全部级联清理
- `deleteMemoryById` 不存在 id：返回 `false`
- `deleteProfile` 命中/未命中返回正确 boolean
- `dimensionInSync` 维度不一致正确返回 `false`

### 红线合规

- ✅ 未修改 `server/src/services/memory.ts`（猫猫写入路径）
- ✅ 未修改 `server/src/db/index.ts`（schema / 迁移）
- ✅ 未修改 `server/src/routes/index.ts`（P2 再加挂载）
- ✅ 未修改任何 auth 文件
- ✅ `writeMemory` / `upsertProfile` / `getProfile` 仅 import 复用，不改函数体
- ✅ `deleteMemoryById` 承重逻辑完全遵循 SSOT §3.3 删除次序

### 关键决策落地

| 决策 | P1 落地方式 |
|------|-------------|
| FULL OUTER JOIN vs UNION | SQLite 3.49.2 原生支持 FULL OUTER JOIN，直接使用，无需 UNION 兜底 |
| 维度一致守卫 (D1) | 独立函数 `dimensionInSync()`，P2 路由层调用 `writeMemory` 前校验 |

## 下一阶段 P2 预览

P2 负责创建 `server/src/routes/owners.ts`（7 个端点，全部走 `requireSession` 管理员鉴权）+ 在 `routes/index.ts` 挂载。

## 续接提示词

```
以 plan 模式继续推进 NyaaLibrary-MCP V2 前端显示改造的 P2 阶段（后端路由）。

必读文档：
- NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md（SSOT，§4.2 路由设计）
- NyaaLibrary-MCP\.docs\V2前端显示改造\阶段交接-001.md（本文件）
- NyaaLibrary-MCP\CLAUDE.md（项目规则）

当前进度：P1 完成 — adminMemory.ts 已就绪（5 函数 + 2 类型），位于 server/src/services/adminMemory.ts。所有 5 个函数已验证通过。

P2 任务：
1. 创建 server/src/routes/owners.ts — 7 个端点（GET/POST/DELETE），统一前缀 /api/owners，use requireSession
2. 在 server/src/routes/index.ts 中 requireSession 之后追加 api.use("/owners", ownersRouter)
3. 维度一致守卫：POST memory 前调用 dimensionInSync()，不一致拒写透传错误
4. 入参 zod 校验（对标 routes/kb.ts 风格）
5. 不改动任何现有文件（除 routes/index.ts 只加 1 行）

关键约束：
- 绝对不改 memory.ts / db/index.ts / auth/memory.ts
- ownerKey 使用 encodeURIComponent
- 所有错误透传原文到前端
- 管理员鉴权与猫猫成对 token 完全隔离
```
