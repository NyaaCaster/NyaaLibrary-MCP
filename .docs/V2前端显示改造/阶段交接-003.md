# NyaaLibrary-MCP V2 前端显示改造 阶段交接 003

## 交接目的

记录 P3 阶段的完成状态，为下一阶段 P4（前端导航+owner 列表页）提供续接上下文。

**续接前必读文档**：
- `NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md`（SSOT §5.3–§5.4）
- `NyaaLibrary-MCP\.docs\V2前端显示改造\阶段交接-002.md` — P2 交接（后端路由完成状态）

## 当前进度

P1 ✅ → P2 ✅ → P3 ✅ — 前端 API 层完成，3 类型 + 7 函数全部就绪。

### 已实现（P3）

| 交付物 | 文件 | 行数 |
|--------|------|------|
| `OwnerSummary` 类型 | `frontend/src/lib/api.ts` | +6 行 |
| `MemoryEntryRow` 类型 | 同上 | +8 行 |
| `ProfileRow` 类型 | 同上 | +7 行 |
| `fetchOwners()` | 同上 | +3 行 |
| `fetchOwnerMemory(ownerKey)` | 同上 | +5 行 |
| `createOwnerMemory(ownerKey, content, salience?)` | 同上 | +6 行 |
| `deleteOwnerMemory(ownerKey, id)` | 同上 | +5 行 |
| `fetchOwnerProfile(ownerKey)` | 同上 | +5 行 |
| `updateOwnerProfile(ownerKey, data)` | 同上 | +8 行 |
| `deleteOwnerProfile(ownerKey)` | 同上 | +5 行 |

### 验证结果

- TypeScript 编译零错误
- Vite production build 通过（1659 modules, 3.65s）
- 所有路径参数使用 `encodeURIComponent(ownerKey)`
- 返回类型与后端 service 层完全一致

### react-query key 约定（供 P4–P6 使用）

```
["owners"]                          — fetchOwners
["owner-memory", ownerKey]         — fetchOwnerMemory
["owner-profile", ownerKey]        — fetchOwnerProfile
```

失效规则：
- 补充/删除记忆 → invalidate `["owner-memory", ownerKey]` + `["owners"]`
- 编辑/删除画像 → invalidate `["owner-profile", ownerKey]` + `["owners"]`

## 下一阶段 P4 预览

P4 负责前端导航改造 + owner 列表页：
1. `AppShell.tsx` 顶栏加「猫猫记忆」导航入口（`Brain` 图标，路由高亮 indigo）
2. `App.tsx` 新增 `/owners` 和 `/owners/:ownerKey` 路由
3. `OwnersPage.tsx` — 卡片网格（复用 `KnowledgeBasesPage` 样式），使用 `useQuery(["owners"], fetchOwners)`

## 续接提示词

```
以 plan 模式继续推进 NyaaLibrary-MCP V2 前端显示改造的 P4 阶段（前端导航+owner 列表页）。

必读文档：
- NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md（SSOT，§5.1–§5.2）
- NyaaLibrary-MCP\.docs\V2前端显示改造\阶段交接-003.md（本文件）
- NyaaLibrary-MCP\CLAUDE.md（项目规则）

当前进度：P1+P2+P3 完成 — 后端全链路（service+路由）和前端 API 层（3类型+7函数）就绪。

P4 任务：
1. AppShell.tsx 顶栏加导航：当前 KB 路由高亮 + 新增「猫猫记忆」入口（lucide-react Brain 图标）
2. App.tsx 新增 /owners 和 /owners/:ownerKey 路由
3. 新建 OwnersPage.tsx：卡片网格布局，useQuery(["owners"], fetchOwners)，每张卡片显示 nickname/owner_key/记忆条数/画像状态
4. 对标 KnowledgeBasesPage.tsx 的卡片样式和空态/加载态

关键约束：
- react-query key: ["owners"]
- 复用现有组件：Pagination、Modal、AppShell 布局
- lucide-react 图标：Brain（猫猫记忆）、Library（知识库）
```
