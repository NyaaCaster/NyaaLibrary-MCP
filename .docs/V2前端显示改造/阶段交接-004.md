# NyaaLibrary-MCP V2 前端显示改造 阶段交接 004

## 交接目的

记录 P4 阶段的完成状态，为下一阶段 P5（记忆条目 Tab）提供续接上下文。

**续接前必读文档**：
- `NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md`（SSOT §5.1–§5.4）
- `NyaaLibrary-MCP\.docs\V2前端显示改造\阶段交接-003.md` — P3 交接

## 当前进度

P1 ✅ → P2 ✅ → P3 ✅ → P4 ✅ — 前端导航 + owner 列表页完成。

### 已实现（P4）

| 交付物 | 文件 | 变更 |
|--------|------|------|
| 顶栏导航（知识库 + 猫猫记忆） | `components/AppShell.tsx` | +15 行 |
| `/owners` + `/owners/:ownerKey` 路由 | `App.tsx` | +4 行 |
| Owner 卡片列表页 | `pages/OwnersPage.tsx` | 86 行（新建） |
| Owner 详情页骨架（P5 填充） | `pages/OwnerDetailPage.tsx` | 65 行（新建） |

### 导航栏

```
[Library] NyaaLibrary  │  知识库  猫猫记忆  │  [🌙] [🚪]
```

- `navLink()` 辅助函数：当前路由匹配时 indigo 高亮（含子路由前缀匹配）
- Brain 图标（lucide-react）用于猫猫记忆入口

### OwnersPage 卡片

每张卡片展示：
- Brain 图标 + nickname（有昵称时下方显示 owner_key）
- 记忆条数（MessageSquare 图标）+ 画像状态（User 图标，"有画像"/"无画像"）
- 点击跳转 `/owners/:ownerKey`

复用 KnowledgeBasesPage 的：
- 卡片网格布局（`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`）
- 加载/空态/错误态（Loader2 动画、虚线边框空态、rose 错误提示）
- Tailwind 卡片样式（`rounded-xl border hover:border-indigo-300`）

### 验证结果

- TypeScript 编译零错误
- Vite production build 通过（1661 modules, 3.23s）
- 代码签名（3 处 BLESSING）不受影响

## 下一阶段 P5 预览

P5 负责 OwnerDetailPage 的记忆条目 Tab（MemoryTab）：
1. 将现有 OwnerDetailPage 骨架改造为带 Tab 的详情页（对标 KbDetailPage）
2. MemoryTab：搜索 + 分页 + 表格 + 查看弹窗 + 内联删除（对标 DocumentsTab）
3. MemoryCreateModal：多行文本框录入记忆 + 可选 salience
4. MemoryDetailModal：只读内容展示 + 内联确认删除

复用组件：`Pagination`、`useSort`、`Modal`、`SortHeader`、`formatDateTime`

## 续接提示词

```
以 plan 模式继续推进 NyaaLibrary-MCP V2 前端显示改造的 P5 阶段（记忆条目 Tab）。

必读文档：
- NyaaLibrary-MCP\.docs\V2前端显示改造\开发计划-SSOT.md（SSOT，§5.1 复用映射 + §5.4 记忆条目 Tab）
- NyaaLibrary-MCP\.docs\V2前端显示改造\阶段交接-004.md（本文件）
- NyaaLibrary-MCP\CLAUDE.md（项目规则）

当前进度：P1+P2+P3+P4 完成 — 后端全链路 + 前端 API + 导航 + owner 列表全部就绪。OwnerDetailPage 已有骨架（显示 owner key + 记忆条目列表）。

P5 任务：
1. 改造 OwnerDetailPage.tsx：加 Tab 栏（对标 KbDetailPage 的 -mb-px 下划线 tab）
2. 新建 MemoryTab.tsx：搜索框 + Pagination + 表格（内容摘要/字符数/salience/时间）
3. 新建 MemoryDetailModal.tsx：readonly textarea + 内联确认删除（DELETE memory/:id）
4. 新建 MemoryCreateModal.tsx：多行文本录入 + 可选 salience，提交后用 fetchOwnerMemory 刷新
5. Tab「记忆条目」（默认）+ Tab「用户画像」（P6 占位）

复用样板文件：
- frontend/src/pages/kb/DocumentsTab.tsx（搜索+分页+表格模式）
- frontend/src/pages/kb/ChunkDetailModal.tsx（Modal + readonly textarea + 内联删除）
- frontend/src/components/Pagination.tsx / useSort.ts / SortHeader.tsx / Modal.tsx / format.ts

关键约束：
- react-query key: ["owner-memory", ownerKey]
- 删除后 invalidate ["owner-memory", ownerKey] + ["owners"]
- 补充后刷新列表
- 使用 encodeURIComponent(ownerKey)
```
