我是Nyaa，保持以中文与我沟通。

我要使用本框架在这里建立一个含前端内容管理页面的MCP服务项目：
- meta信息
	- 项目名称：NyaaLibrary-MCP
	- 项目概述：用于LLM聊天知识库的MCP
	- 开发者名：NyaaCaster
	- 隐性代码签名："Nyaa be with you."
- GitHub仓库地址：https://github.com/NyaaCaster/NyaaLibrary-MCP.git

按照NyaaFrame框架模板进行初始化流程

前端页面设计：
- 前端页面：知识库、文档的管理和录入页面，嵌入模型API接入等功能
	- 知识库
		- `创建知识库`按钮
		- `知识库`条目
			- 名称
			- 描述
			- 文档数量
			- 分块数量
			- `重命名`图标按钮（修改名称和描述）
			- `删除`图标按钮（删除需要确认）
		- `知识库详情`界面
			- 概述
				- 基本信息
					- 名称
					- 描述
					- 创建时间：YY/MM/DD hh:mm
					- 更新时间：YY/MM/DD hh:mm
				- 统计信息
					- 文档数量
					- 分块数量
			- 文档管理
				- `上传文档`按钮
					- `上传文档`界面
						- 文档置入区：拖放文件到这里或点击打开文件
							- 支持的格式：.txt, .md, .pdf, .docx, .epub, .xls, .xlsx
							- 单个最大文件大小：128MB（.env预设）
							- 单次最多上传个数：10（.env预设）
						- 分块设置
							- 分块大小：每个文本块的字符数，默认512（.env预设）
							- 分块重叠：相邻文本块之间的重叠字符数，默认50（.env预设）
						- 批处理设置
							- 批处理大小：每批处理的文本数量，默认32（.env预设）
							- 并发任务限制：并发任务数量限制，默认3（.env预设）
							- 最大重试次数：失败时的最大重试次数，默认3（.env预设）
				- `搜索文档`过滤栏：单行输入文档，模糊匹配
					- `取消筛选`图标按钮
				- 文档列表表格
					- 每个文档条目（每页10条）
						- 文档名称（点击切换升降序）：文档格式svg图标+`文档名.格式扩展名`
						- 文档格式（点击切换升降序）
						- 文档大小（点击切换升降序）：最大2位小数，单位支持B、KB、MB
						- 分块数（点击切换升降序）
						- 上传时间（点击切换升降序）：YY/MM/DD hh:mm
						- `查看文档`图标按钮
							- 文档信息
								- 文档名称
								- 文档格式
								- 文档大小
								- 分块数
								- 上传时间
							- 分块列表
								- 每个分块条目（每页10条）
									- 分块序号（例如：#1）
									- 分块内容（单行显示，固定列宽，超长后末尾显示"..."然后截断）
									- 字符数（例如：456 字符）
									- `查看分块`图标按钮
										- 分块详情
									- 分块序号
									- 字符数
									- 向量ID（例如：f8698beb-410c-4b73-9a77-6ed2e89f0cdf）
									- 分块内容：多行文本框，带滚动条
									- `删除分块`图标按钮
								- 条目位置计数（例如：11-20 of 120）
								- `首页`图标按钮
								- `上一页`图标按钮
								- `下一页`图标按钮
								- `尾页`图标按钮
						- `删除文档`图标按钮
					- 条目位置计数（例如：11-20 of 120）
					- `首页`图标按钮
					- `上一页`图标按钮
					- `下一页`图标按钮
					- `尾页`图标按钮
			- 知识库检索（使用稠密检索和稀疏检索测试知识库内容）
				- 检索差内容：输入框多行文本输入框
				- 检索设置
					- 返回结果数量：最多返回多少条检索结果，默认5（.env预设）
				- `检索`按钮
				- 检索结果
					- 检索结果计数
					- 检索结果条目
						- 结果编号（例如：#1）
						- 文本块编号（例如：文本块 #0）
						- 所属文档名称（例如：文档名.格式扩展名）
						- 字符数（例如：666 字符）
						- 相关度分数（例如：相关度分数 0.0320）
						- 文本块内容：多行文本框，带滚动条
			- 设置
				- 基本设置
					- 分块大小
					- 分块重叠
				- 检索设置
					- 稠密检索数量：默认50（.env预设）
					- 稀疏检索数量：默认50（.env预设）
				- `保存设置`按钮
	- 嵌入模型设置
		- API Base URL
		- API Key
		- 模型名称
		- 嵌入维度：嵌入向量的维度。
			- `自动获取`按钮，点击后通过api拉取模型meta信息获得入向量的维度值自动填入`嵌入维度`文本框。
		- `取消`按钮
		- `保存设置`按钮
	- `颜色主题`图标按钮
		- 淡色
		- 深色
		- 跟随系统（默认）
	- `退出登录`图标按钮

- 前端页面需要账号密码登录，浏览器本地记录登录状态，账号密码唯一，在 .env 文件中记录。
- 前端页面布局需要兼容PC横屏和手机竖屏两种布局
- 请对页面进行现代化的展示和交互设计

- MCP后端设计
	- MCP后端请参考我另一个项目 H:\GitHub\NyaaChat-MCP 的技术栈、标准以及鉴权方式
	- MCP后端MCP保持apikey鉴权方式，MCP_API_KEY生成在 .env 文件中
	- 寻找通用的知识库MCP实现方案

- docker容器端口使用5101（.env设置）

建立项目蓝图和备忘，维持任务蓝图和备忘更新

整理readme，包含类似这样的mcp接入样例：
```
{
  "type": "streamableHttp",
  "url": "http://localhost:5101/mcp",
  "headers": {
    "Authorization": "Bearer xxx...."
  }
}
```

完成初github仓库始化和初次提交推送